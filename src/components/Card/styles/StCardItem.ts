import styled from 'styled-components';

export const StCardItem = styled.li`
	font-size: var(--fs-sm);
	line-height: 1.5;
	font-weight: var(--fw-light);

	& > span {
		font-weight: var(--fw-bold);
	}
`;
